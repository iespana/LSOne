
namespace LSOne.DataLayer.DataProviders.Inventory
{
    /// <summary>
    /// A enum that defines sorting for the ReasonCode rows
    /// </summary>
    public enum ReasonCodeSorting
    {
        ReasonId,
        ReasonText,
        Action,
        BeginDate,
        EndDate,
        SystemReason,
        ShowOnPos
    };
}